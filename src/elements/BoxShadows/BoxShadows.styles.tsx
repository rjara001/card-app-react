import { makeStyles } from "@material-ui/styles";
import { Theme } from "@mui/material/styles";

export const useBoxShadowsStyles = makeStyles((theme: Theme)=>({
    root: {
        justifyContent: 'center'
    }
}));

